

pipeline {
    agent any
parameters {
  string defaultValue: 'Mr Jenkins ', description: 'who should i say hello to ?', name: 'Person', trim: true
}
    stages {
        stage('Build') {
            steps {
                echo "Hello ${params.Person}"
            }
        }
    }
}       
        