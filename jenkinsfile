pipline {
    agent any
    stages (build) [
        steps (

        )

    ]
    stages (test) [
        steps (

        )
    ]
    stages (deploy) [
        steps (
            
        )
    ]
}