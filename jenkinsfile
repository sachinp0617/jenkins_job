pipeline {
    agent {
           label 'slave2'
        }
    stages {
        stage ('build') {
            steps {
                sh '''
                echo 'this is build '
                '''
            }
        }
        stage ('deploy') {
            steps {
                sh '''
                echo 'this is deploy'
                '''
            }
        }
    }
    
}
